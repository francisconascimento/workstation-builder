---

- hosts: localhost
  connection: local
#  vars:
#    whoami: "{{ lookup('env', 'USER') }}"
#    user_home: "{{ lookup('env', 'HOME') }}"

  roles:
    - role: basic
    - role: editors_and_environment
    - role: python
    - role: email_and_internet